{
  "name": "google-notebook",
  "version": "1.0.0",
  "description": "A Google Notebook-style note-taking app with AI integration",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "server": "node server.js",
    "install-all": "npm install && cd frontend && npm install",
    "start": "concurrently \"npm run server\" \"npm run dev\"",
    "setup": "npm run install-all && echo 'Setup completed! Now add your API key to .env file and run: npm start'",
    "check": "curl http://localhost:3001/api/health || echo 'Backend not running'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "@google/generative-ai": "^0.2.1"
  }
}